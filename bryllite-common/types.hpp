#pragma once

#include <vector>
#include <map>
#include <array>
#include <string>
#include <cstdio>
#include <cstdlib>
#include <cstdint>

#include <boost/multiprecision/cpp_int.hpp>

using byte = uint8_t;
//using bytes = std::vector<byte>;

//using bigint = boost::multiprecision::number< boost::multiprecision::cpp_int_backend<> >;
//using uint256 = boost::multiprecision::number< boost::multiprecision::cpp_int_backend<256, 256, boost::multiprecision::unsigned_magnitude, boost::multiprecision::unchecked, void> >;
//using int256 = boost::multiprecision::number< boost::multiprecision::cpp_int_backend<256, 256, boost::multiprecision::signed_magnitude, boost::multiprecision::unchecked, void> >;

//using string32 = std::array<char,32>;


